What are HTML Entities?
HTML entities are special characters reserved in HTML that are represented as codes. These codes are used to display symbols that cannot be typed directly into the HTML document or to prevent characters from being interpreted as HTML syntax.

5 Commonly Used HTML Entities:

&amp; → &
&lt; → <
&gt; → >
&quot; → "
&apos; → '